#include<stdio.h>
#include<string.h>

int main(){
    // char str[] = "aeroplane";
    // printf("%d",*(str+strlen(str)));
    // printf("%c",*(str+strlen(str)));
    // return 0;
    // char far *far1,*far2;
    // printf("%d %d",sizeof(far1),sizeof(far2));

    // int i=3, *j, k;
    // j = &i;
    // printf("%d\n", i**j*i+*j);//3*3*3+3

    // int x=30, *y, *z;
    // y=&x; /* Assume address of x is 500 and integer is 4 byte size */
    // z=y;//30
    // *y++=*z++;//31
    // x++;
    // printf("x=%d, y=%d, z=%d\n", x, y, z);

    // char x[] = "God";
    // int i;
    // for ( i = 0; x[i]!='\0'; i++)
    // {
    //     printf("%c %c %c %c ",x[i],*(x+i),*(i+x),i[x]);//i
    // }


    char *p  = "Hello";
    printf("%s ",)
    
    return 0;
}